#include <iostream>
#include <cmath>

using namespace std;

int main(){
    double a, b, c, d;
    

    cout << "Ingrese el angulo en grados: " << endl;
    cin >> a;

    b = sin(c);
     c = (a * 3.1415926535) / 180;

    cout << "El angulo en R es: " << c << endl;
    cout << "El seno del angulo es: " << b << endl;
    
    return 0;
}
